# Normalize all text files to LF on all platforms.
# text=auto lets git detect binary vs text; eol=lf enforces LF for text files.
* text=auto eol=lf

# Binary files — never normalize
*.exe binary
*.png binary
*.jpg binary
*.gif binary
